# {{ ansible_managed }}
127.0.0.1   localhost
{% for host in groups['elasticsearch_all']['group']] %}
{{ hostvars[host]['ansible_eth1']['ipv4']['address'] }} {{ hostvars[host]['ansible_nodename'] }}
{% endfor %}
